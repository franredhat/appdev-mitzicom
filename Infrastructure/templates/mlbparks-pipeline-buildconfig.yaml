kind: "BuildConfig"
apiVersion: "v1"
metadata:
  name: "mlbparks-pipeline"
spec:
  strategy:
    jenkinsPipelineStrategy:
      jenkinsfile: https://raw.githubusercontent.com/franredhat/appdev-mitzicom/master/Nationalparks/Jenkinsfile
    type: JenkinsPipeline
